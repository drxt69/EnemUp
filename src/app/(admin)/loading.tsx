import { PageLoading } from "@/components/ui/page-loading";

export default function AdminLoading() {
  return (
    <PageLoading
      title="Carregando administração"
      subtitle="Atualizando os indicadores da plataforma."
    />
  );
}
